# This is my local-repo
<br>
1. I leanr many git commands like:
<br>
ls (Used for check list)
<br>
ls -Hidden (Used for check hidden files)
<br>
cd <-Any folder name->
<br>
cd .. (For exit any folder in which we have)
<br>
git clone <-Any link->
<br>
git add . (add code or lines in terminal)
<br>
git status (For checking GIT status)
<br>
git commit -m "Any commit message" (It is very important. we write the change that we do in our file or code)
<br>
git push origin main (For pushing our code in main file on GIT account)
<br>
git push origin <-branch name-> (For pushing the code on GIT HUB)
<br>
git branch (For checking in which branch we are in)
<br>
git branch -M <-Any name-> (For renama the branch name)
<br>
git checkout (For check in which file we change and whats its status)
<br>
git checkout main (for switching main branch)
<br>
git checkout <-Any branch name-> (For switching any branch which name we enter)
<br>
git checkout -b <-Any name for create new branch-> (For craete new branch)
<br>
git checkout -d <-Any branch name for delete-> (For deleting any branch)
<br>
mkdir <-Any follder name-> (For make new folder with the help of terminal and command)
<br>
git init (Used to convert any folder or file to make them in git file)
<br>
git remote add origin <-Any link which we get from GIT new Repository-> (For add new origin in GIT)
<br>
git remote -v (For verify the remote)
<br>
git diff <-branch name-> (For checking difference)
<br>
git pull origin main (For move our remote controls or code in our local or pc system)
<br>
git merge <-Branch name where we are not stand-> (For merge git branches)
<br>
git push <-For pushing our code to our git account->